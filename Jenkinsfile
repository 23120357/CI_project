pipeline {
    agent any

    stages {
        stages('Build') {
          steps {
              echo 'Run Build on test branch'
            }
        }
    }

    stages {
        stages('Test') {
          steps {
              echo 'Run Test'
            }
        }
    }
}
